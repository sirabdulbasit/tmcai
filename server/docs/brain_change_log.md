# Brain Change Log

**Version:** v3.5 · **Last updated:** 2026-08-05 22:45 PKT

**One of three governing documents** (owner ruling, 2026-08-05):

| Doc | Job |
|---|---|
| **`brain_change_log.md`** ← this file | Errors faced/recorded → solution provided → **deployed?** |
| `brain_chat_log.md` | Record of every chat log received (`CL-NNN`) |
| `brain_evaluation_chart.md` | Are we moving ahead? Per-deploy before/after impact |

## Rules

1. Every error the owner reports gets a **`DEF-NNN`** row here in the same session, with the
   date-time reported. Nothing is carried in conversation only.
2. A row is complete only when it has: **root cause** (not a restated symptom), **solution**,
   **fix commit**, **deployed date-time**, and **verification** (CI / LIVE).
3. `DEPLOYED` ≠ `VERIFIED`. A fix on production that nobody has exercised is
   `deployed, unverified` and must say so.
4. Symptom tags come from the existing vocabulary so recurrence is detectable — see the
   evaluation chart's recurrence table.

**Status vocabulary:** `OPEN` · `FIXED (not deployed)` · `DEPLOYED (unverified)` ·
`VERIFIED (CI)` · `VERIFIED (LIVE)` · `REOPENED`

---

## A. Resolved

| DEF | Reported | Error (owner-visible) | Root cause | Solution | Commit | Deployed | Status |
|---|---|---|---|---|---|---|---|
| DEF-001 | 07-07 | Empty "With:" line on a meeting for a non-contact | ad-hoc attendee email dropped by the parser | retain ad-hoc emails/phones in `normaliseAction` | `682bbc1` | 07-07 | VERIFIED (CI) |
| DEF-002 | 07-08 | "The email has been sent to Asad" — never sent | passive-voice completion claim escaped a first-person-only guard | widen `claimsCompletion` to passive/third person | `259f972`, `459bd4d` | 07-08 | VERIFIED (CI) |
| DEF-003 | 07-08 | "send a test email…" answered `[noted]` | stale prompt captured a command | imperative detection + marker sanitiser | `c977a3d` | 07-08 | VERIFIED (CI) |
| DEF-004 | 07-10 | Proposed messaging Asad about Yousaf's item | prompt block gave owner NAME but no routable id → substitution | bind routable id; forbid closest-match | `680c441` | 07-10 | VERIFIED (CI) |
| DEF-005…010 | 07-10→07-16 | see archive Chats 5–10 | various | various | — | — | VERIFIED (CI) |
| DEF-011 | 07-17 | Voice pipeline silent, no processing state | no observable activity signal | activity layer (reaction + presence) | — | 07-17 | VERIFIED (CI) |
| DEF-012 | 07-17 | `@lid` activity rejected; PTT unavailable | chat-state helpers rejected the LID Wid | phone-chat retry limb | — | 07-17 | VERIFIED (CI) |
| DEF-013 | 07-22 | "Whatsup?" answered `[blocker recorded]` | a regex allowlist was the final decision boundary | LLM relevance gate ahead of `recordAnswer` | `741d907` | 07-22 | VERIFIED (CI) |
| DEF-014 | 07-27 17:25 | Delegation sends refused **6 days**; `[no tenant whatsapp channel configured]` | probe echo compared against ONE Wid spelling; then 3 in-memory flags self-locked with no exit | `@lid`-aware echo set; `recordOutboundProof` re-arms; `repairReason` splits liveness from pairing | `5e3f3c1` | 07-31 | **VERIFIED (LIVE)** 07-31 |
| DEF-015 | 08-03 | Yousaf's reply vanished; Brain claimed "connection degraded" | inbound door resolved counterpart phone via `getContact()` only, which throws on `@lid` | shared `waIdentity.lidToPhone` as second limb | `72ed7f6` | 08-04 | DEPLOYED (unverified) — needs a counterpart reply |
| DEF-016 | 08-04 | **Voice notes unreadable** ("I could not read that voice note") | `downloadMedia` resolves the message from an id that EMBEDS `@lid`; the lookup throws before any network call | `webjsMediaDirect` — locate by string compare, then run the library's own download | `889edc0`, `b11fa3c` | 08-04 | **VERIFIED (LIVE)** 08-04 19:37 |
| DEF-023 | 08-04 20:00 | Confirmed 3-step plan died: `[Unknown pending action kind: updateopenitem]` — all updates lost | `update_open_item` was registry-valid, validated, previewed and confirmed but had NO dispatcher case | shared `applyOpenItemUpdate`; missing case added; `DISPATCHABLE_PLAN_STEP_KINDS` guard rejects undispatchable steps before confirmation | `666fb2a` | 08-05 11:40 | **VERIFIED (LIVE)** 08-05 14:41 — 3 of 3 plan steps dispatched and persisted |
| DEF-024 | 08-03 | `[actionplan failed to queue]`, then **08-04 20:25 the infinite preview loop** — "send" twice, same preview back, nothing dispatched | legacy `UNIQUE(user_id,channel,status)`; `startPending` throws, compose swallows it, **no pending row persisted, so "send" has nothing to confirm** and reasoning re-proposes forever. The 20260522 migration documents this EXACT symptom but dropped a **guessed** constraint name and silently no-op'd for 74 migrations | migration matching by COLUMN SET not name; `startPending` survives the collision by deleting stale rows | `76739af` | **08-05 11:40** | **VERIFIED (LIVE)** 08-05 14:41 — pending persisted, "send" dispatched, and a follow-up query confirmed all three rows in the DB |
| DEF-025 | 08-04 | No native typing/recording indicator on any turn | `sendChatstate` calls generic `createWid()` on an `@lid` id; WhatsApp keeps LID constructors separate | `webjsChatStateDirect` picks the constructor by domain | `c1e5ceb` | 08-05 11:40 | DEPLOYED (unverified) |
| DEF-017 | 08-03, 08-04 | Compound commands partially consumed: "Priority High, due date today and delegate to Hamna Latif" → deadline **2024-03-29**, junk task titled "Priority High", delegation silently dropped | The relevance verdict was BINARY over the whole message — swallow all or drop all. A compound instruction answered the prompt with its first clause, so the ENTIRE string was consumed and fed to the date parser. The pre-existing piggyback rescue could not save it: it routes leftovers to a SECOND, weaker extractor whose intent vocabulary has no open-item delegate. Two reasoning paths instead of one | New `partially_answers` verdict carrying `answerPart` + `residual`; only the answering half is recorded and fed to the side effect; the residual is routed through the **same** compose path every other turn uses. Plus a sanity window on `parseDuePhrase` (5 callers, previously unguarded) so a past/absurd deadline is refused instead of written | `4ec5d99` | 08-05 12:02 | **VERIFIED (LIVE)** 08-05 11:53 — one voice note became a correct 5-step plan |
| DEF-018 | 08-04 19:57, again 20:25 | Preview unconfirmable: "update open item" ×3, then "Delegate item to Hamna Latif Bhutta" ×3 — no item names, so three indistinguishable approvals | `renderPlanPreview` had no case for either kind; the default branch stringifies the bare kind | explicit cases naming the item TITLE plus every changed field; tenant+user scoped lookup | `7ace0f5` | 08-05 11:40 | **VERIFIED (LIVE)** 08-05 11:53 — preview named every item and field |

| DEF-032 | 08-05 11:54 | A dictated 5-step plan was **silently displaced** by a follow-up turn; "send" then dispatched the replacement (a test email) instead of the plan, and the plan was lost | one-active-pending-per-channel cancels an unconfirmed pending with no user-visible signal, so "send" silently changes meaning | `startPending` now reports what it displaced; the new preview carries a visible ⚠️ notice naming what was replaced and that "send" applies to the new thing | `1880538` | 08-05 ~13:00 | **VERIFIED (LIVE)** 08-05 13:03 — CL-023 shows the displacement notice firing on the owner's own "send". It worked exactly as designed and is what exposed DEF-035 |
| DEF-033 | 08-05 11:55 | "You email them" (about three delegated items) produced a **canned "Test email from Nexeo"** sent to a real colleague from the owner's own address | the prompt's test-email auto-fill exception said "or similar", and the LLM over-applied it to a vague email request | exception now requires the user to literally say "test"; template forbidden elsewhere; must ask or compose from the real subject | `1880538` | 08-05 ~13:00 | DEPLOYED (unverified) — no email request exercised since |

| **DEF-035** | 08-04 20:25, 08-05 13:05 | **"send" → same preview → "confirm" → same preview.** Nothing ever dispatched; the owner could not complete a single delegation | **ORDERING.** The reasoning gate ran FIRST in `compose` and returned early with a preview whenever it decided to act. The pending/confirm reducer sat ~200 lines below and was therefore UNREACHABLE on a confirmation turn: "send" was fed back into reasoning, which re-proposed the identical plan, called `startPending` (displacing the plan just approved) and re-rendered the preview. My DEF-032 displacement warning is what finally exposed it | An early-confirm guard placed BEFORE the reasoning gate: if a `preview_shown` pending exists and the turn is an unambiguous confirmation, dispatch the STORED slots and never re-reason. Confirmation beats re-planning | `1d4a1d3` | **08-05 13:44** | **VERIFIED (LIVE)** 08-05 14:41 — "send" dispatched three real delegations after three sessions of looping. The loop is dead |

| **DEF-039** | 08-05 14:00 (external audit) | **The DEF-035 fix could dispatch the same external action TWICE** — a double-tapped "send" or a retried WhatsApp webhook would send duplicate emails/WhatsApp messages to real counterparts. Second, silent half: early-confirmed dispatches never left `previewed` in the artifact ledger, so Brain — which reads that ledger to answer "did you do it?" — would report nothing happened right after acting (DEF-034's family) | **The guards lived INSIDE the branch they protected.** The legacy confirm reducer wrapped `dispatchPendingDirect` in `wrapDispatchIdem` and drove the artifact through `confirmed → dispatching → succeeded`. My early-confirm guard deliberately bypassed that branch to fix the ordering — and silently bypassed its protections with it. No test caught it because every existing test exercised the branch that still had them. DEF-035 is what made this reachable at all: before it, "send" never dispatched, so nothing could be duplicated. **The fix for one defect created a worse one** | `dispatchConfirmedPending` extracted as the single chokepoint — artifact transitions + idempotency + terminal marking + throw handling in one place; BOTH confirm paths call it. `dispatchPendingDirect` now has exactly one external caller, CI-enforced by `def039SingleDispatchChokepoint.test.ts` (6 tests). Same containment shape as `waIdentity.ts` for the `@lid` class | `HEAD` | not yet | FIXED (not deployed) — **blocks the DEF-035 live test** |

## B. Open

| DEF | Reported | Error | Root cause (if known) | Proposed solution | Status |
|---|---|---|---|---|---|
| **DEF-041** | 08-05 14:08 | **FALSE STATEMENT SENT TO A REAL PERSON UNDER THE OWNER'S IDENTITY.** Brain said "I will delegate all four unassigned items to Hamna Latif Bhutta now", delegated NOTHING, then emailed Hamna from `basit.ahmed@tmcltd.com`: *"These items have been delegated to you: …"*. Four minutes later, asked to list Hamna's items, it correctly answered she has none | **CORRECTED 14:35 — my first diagnosis was wrong and would have produced the wrong fix.** I said the guard was unreachable below the reasoning path's early returns. It was not: `validateBeforeRender` runs at the route level (`brainAskRoutes.ts:164`) on every path including WhatsApp. The guard was REACHED and defeated twice over. **(1) A path-shaped exemption:** `emptyPromiseEligible = !sourceIsReasoning && …` disabled the rule for every reasoning-sourced answer — the default path since `f8ff5a1`. **(2) A duplicated, diverged regex:** the validator kept its OWN narrower copy under a comment claiming the two were "in sync" and that this one was "canonical". Both false. It was first-person-only — no `i will`, no passive voice — so it matched **neither** sentence in the incident, including the literal email body. DEF-002 widened the composer's copy for passive voice in July; that widening never reached the copy on the live path | Exemption now keyed to the STATE of the turn (structured state, or a dispatch that actually succeeded) instead of the code path — the two conditions on either side already covered every legitimate case the exemption named. The duplicate regex is deleted; `responseValidator` imports the single canonical `EMPTY_PROMISE_RE`. 10 tests in `def041ReasoningExemption.test.ts`, including both verbatim production sentences and a source guard forbidding a second definition or a return of the path check | `HEAD` | not yet | **FIXED (not deployed)** — CRITICAL |
| **DEF-038** | 08-05 13:50, hit again 18:43 | **Brain asks permission for what the owner already instructed.** He named the action, named the person, disambiguated her himself one turn earlier — and was still asked to type "send" | Preview-before-dispatch was a BLANKET rule. It was the containment for a Brain that could not ground targets (DEF-002, DEF-004), a job `actionTargetGuard` now does in CI. The manual gate outlived its reason — and became the home of DEF-024, DEF-032, DEF-035 and DEF-055: **four defects in one family, two introduced while fixing the one before** | `confirmationPolicyService.assessConfirmationNeed` — the model decides, given FACTS from the DB (has this person been contacted before, is it reversible, whose identity is on it). Confirm only when the check-in tells the owner something he does not know, and NAME that thing; never "shall I proceed?". **Fails closed** on error, low confidence or an unresolvable recipient — a wrong send costs a real message, a wasted question costs a tap. Novelty decays with history, so a proven Brain asks less over time without that being a separate feature | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-059** | 08-05 18:44 (owner) | *"why it sent 'WhatsApp accepted the message… did not return a receipt ID'"* — plumbing language in a human conversation, framing a successful send as a partial failure | The wording existed because nothing read the delivery ticks, so the send path genuinely did not know the outcome | Now `Message sent to <first name>.` Delivery precision comes from the ack ledger (DEF-052) when asked, not guessed at send time. Owner-specified outbound template implemented as ONE renderer for every channel (`outboundMessageTemplate.ts`): `Hi,` / body / blank / `<brainName>` / `Assistant <userName>` — both names resolved per user, nothing hardcoded | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-052** | 08-05 17:20 (owner) | *"whatsapp shows single tick sent, double tick received — why don't it read it?"* Every send ended "did not return a receipt ID", so *"did you inform her?"* stayed permanently a guess | `grep message_ack services/whatsapp/` returned **nothing**. The library emits an ack on every message (1 sent · 2 delivered · 3 read) and we subscribed to none of it. The uncertainty was self-inflicted — the answer arrives seconds later on an event nobody listened for | `message_ack` listener + `outboundAckService`. Records the highest state seen against `whatsapp_messages`, monotonic so a late lower ack cannot downgrade `read` to `delivered`; `fromMe` only; fully guarded — an ack is observability and must never disturb the pipeline. Gives DEF-037 its first real ledger fact: level 2 = on her phone, level 3 = she opened it | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-049** | 08-05 15:00 (owner) | Owner asked Brain to introduce itself as an AI assistant to counterparts. Two prompts instructed the exact opposite — *"NEVER mention MyOS, Brain, AI, or any automation — the reply must read as if the user wrote it"* — one of them on the **autonomous** reply path | An instruction to impersonate the owner, contradicting the standing rule that Brain never speaks as the user. The disclosure footer (*"Sent by Nexeo, <user>'s AI assistant"*) already existed and was fighting these prompts | Both removed; both now require a truthful answer if a recipient asks whether it is automated, and forbid claiming to be a person. **Still open:** identify once per person on first contact, and say what happens to their reply | `HEAD` | not yet | **FIXED (not deployed)** — first-contact introduction now shipped: a stranger gets one line naming who is writing and that their reply reaches the owner; everyone after that just gets the message. State kept on the contact (`metadata.introducedAt`), marked only after a confirmed send, and fails toward introducing |
| **DEF-060** | 08-05 22:35 | **"brain send message to Hamna and she responded to Brain but it didnt notify me."** No notification about ANY unknown sender since 2026-07-31 — `brain_prompt_queue` had no new row in five days | **A one-word literal, hidden by three separate things.** `triageUnregisteredInbound` enqueued with `criticality: 'normal'`; the type is `'routine' \| 'high' \| 'top'` and the DB enforces it, so every insert died with Postgres **23514** (check-constraint violation) and the inbound was "held". Invisible because (1) `as any` on the call suppressed the compile error, (2) the catch swallowed it into a `warn` line the owner never sees, (3) the queue was the ONLY route to him, so its failure meant total silence rather than degraded service. Note this is NOT the DEF-054 dispatcher — production already had that; nothing was ever written for it to send | `'routine'` (never `'high'`: `channelForCriticality` escalates that to a voice note or business call, and a wrong number must not ring the owner). `as any` removed so the compiler catches the next one. **Fallback added:** when the queue rejects, relay direct via `brainContactsUser`, which does not touch `brain_prompt_queue` and so survives exactly this failure class; a fallback that also fails logs at ERROR. `ownerUserId`/`senderName` hoisted out of the `try` — scoped inside, the code could not answer "who do I tell?" at the one moment it mattered | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-058** | 08-05 18:21 | **Brain narrated a capability it did not have.** "Sir, I've created a contact for your friend Arjamand Bano with the number you provided. I've also added a note to her profile." Nothing was created. One minute later, asked to message her: *"I don't have Arjamand Bano in your contacts list"* — the true statement | **There was no `create_contact` action.** Not in the schema, not in the dispatcher, not in the registry. Brain could UPDATE a contact but never CREATE one, so the claim was guaranteed false the moment it was made. And the empty-promise guard could not see it: **"created" was absent from the enumerated verb list** — a list now extended after the fact three times (passive voice July/DEF-002, `i will` 08-05/DEF-041, this), each time the day after a real fabrication reached the owner | `create_contact` built end to end: union, normaliser (refuses a contact with no email AND no phone — unreachable rows are the debris that grew the list to 620), dispatcher, registry seed, prompt schema, preview line. **Carries the DEF-051 collision check** — if the email or phone already belongs to someone it refuses and names them rather than creating a second row. New contacts are `scope: 'user'`, never tenant-visible by default. Applies immediately (contacts nobody). Verb list extended, with the honest note in-code that enumeration can never be complete and the real fix is verifying claims against the dispatch ledger (DEF-037) | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-057** | 08-05 18:15 (owner ruling) | *"make email and phone separate fields"* · *"give me option of pruning at UI also and remove all other options"* | Both partly rested on a false premise I confirmed rather than assumed. `entities.email` and `entities.phone` have **always** been separate columns — no migration was needed; the UI collapsed them into one `EMAIL / PHONE` cell, which is what hid that one Hamna row had only an email and the other only a phone. A UI prune also already existed as `🧠 Smart cleanup` | Email and Phone are now separate columns in the table. Toolbar cut to `+ Add contact` · `Import: Google` · `🧹 Prune`; removed `Import: Microsoft`, `Refresh from feed` (the feed sweep runs automatically) and the `Advanced` escape hatches. Google import kept deliberately — it is how real contacts enter, and auto-discovery alone is what produced 620 rows of noise | `HEAD` | not yet | **FIXED (not deployed)** — no migration |
| **DEF-056** | 08-05 18:05 (owner ruling) | *"nothing should be hardcoded it should handle through smart brain"* — the confirm path decided MEANING with two regexes: a fixed vocabulary (`yes\|send\|ok\|haan\|kar do…`) and, after DEF-055, a second pattern over Brain's last message | Both sat exactly where the standing rule forbids a pattern: at the decision boundary. And both failed the way hardcoded judgement always fails — confidently wrong about meaning. The vocabulary decided that any "yes" anywhere was consent, which dispatched a stale preview to a third party at 18:00 | `resolveAmbiguousWithLlm` — which already takes the message, the pending action AND Brain's last message, and returns `confirm_preview` / `new_task` / `answer_question` / `cancel_pending` — now decides. It has existed for months and the turn reducer 400 lines below has always used it; this guard simply never called it. **What remains is a prefilter only** (a preview is outstanding, and the reply is short enough to be a reference): its sole power is to skip the classifier call and fall through to normal reasoning, so it can never cause a send. Classifier unavailable ⇒ fall through, never guess — a re-plan costs a turn, a wrong guess costs a real message to a real person | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-055** | 08-05 18:00 | **"yes" sent a WhatsApp to the wrong person.** Brain asked *"I can record a note on her contact profile — would you like me to do that?"*; the owner said "yes"; a WhatsApp went to **Hamna Latif Bhutta** from a stale `preview_shown` pending left over from the 17:17 flow, and the note about Arjamand Bano was never written | **My DEF-035 early-confirm guard.** It asked two questions — is this a bare confirmation, does a stored preview exist — and never the one that matters: *is that preview what Brain JUST asked about?* Any bare "yes" could therefore dispatch any outstanding preview, no matter how much conversation had happened since. Fourth defect in the confirm family and the **second I introduced while fixing the previous one** | The preview must be the most recent thing Brain said. If Brain has since asked anything else, the confirmation belongs to that and the turn falls through to normal reasoning. Skips are logged, not silent. **This is a containment patch, not the cure** — the class only closes with DEF-038, which removes the confirmation step for instructed actions altogether | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-054** | 08-05 17:35 | **The brain prompt queue had no consumer.** Owner: *"then the purpose of sending message to Brain to ask from Hamna has no use if brain is not updating me about Hamna's response"* — correct. Every `notifyOwner` path enqueued a notification that was never delivered | `sendNextPrompt` was complete and correct — in-flight guard, criticality ordering, `scheduledAt`, atomic promotion with race loss, quiet-hours rollback, ack id for reply correlation — and was called from **five smoke-test scripts and nothing in production**. Eight production paths enqueued; the only job touching the queue was `expireStalePrompts`, which DELETES after 30 minutes. Notifications were written for the owner and expired unread. **Third producer-with-no-consumer in one day** (DEF-023 dispatcher-less action kind, `autonomous_outbound` "no consumer exists", this). Also explains three threads sitting in `awaiting_reply` for days with nothing surfacing | `dispatchDuePrompts()` drains every user with a queued prompt; registered in `centralActionGovernor` as `brain_prompt_dispatch`, due on every tick (~5 min effective latency, bounded by the governor's own interval — stated plainly in the code rather than claiming the cadence number). One prompt per user per sweep by design: `sendNextPrompt`'s in-flight guard means the queue advances as the owner answers instead of arriving as a burst. A per-user failure never aborts the sweep. 8 tests + registry-parity guard so the consumer cannot silently disappear again | `HEAD` | not yet | **FIXED (not deployed)** — the last link in the ask→answer chain |
| **DEF-053** | 08-05 17:17 | The DEF-048 capability shipped and was **never used**. "Ask Hamna that will she come office tomorrow" was sent as a bare `notify_via_whatsapp` — message delivered, no open item, no thread, so her reply had nothing to attach to. The owner asked a question and could not receive the answer | **Four links, one chain, three broken.** (1) The prompt never distinguished telling from asking; `notify_via_whatsapp` is described for "inform/notify/tell" and the model reasonably matched "ask" to it. (2) Neither advertised action schema declared `delegateeCandidateId`, so the model could not have emitted it even if it wanted to. (3) **`normaliseAction` rebuilt the action field by field and DROPPED the delegatee** — the quiet one: a perfect emission would still have become an untracked note, silently. (4) The preview said "Add open item", understating that a person gets messaged | All four closed. Prompt rule: *"after this message goes out, is the user waiting on something?"* — if yes it is an open item assigned to that person, if no it is a notify. Both schemas advertise the field; the normaliser preserves it; the preview reads `Ask <person>: "<question>" (tracked — I'll report their answer)`. 4 chain-integrity tests, one per link | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-050** | 08-05 16:51 | **"Sir, I don't have contact details for Hamna Latif"** — while her `entities` row held BOTH her email and `+923134199294`. Earlier the same afternoon the model emitted candidateId `cmowq07k70gkqrtk53b8b1a8f`, a well-formed cuid belonging to no row in `entities` OR `wiki_pages` | **A hardcoded cap decided who exists.** `buildCandidatesBlockForReasoning` took `take: 60` ordered by `relationshipStrength`, and built that list **without reference to the question**. Production: **620 contacts, Hamna Latif Bhutta ranks 419** — 560 of 620 invisible on every turn. Shown a list without the named person in it, the model invented a plausible id rather than reporting it could not see her. Two failure modes from one cause: fabricate, or deny. Note `pickToneRecipientsFromMessage` forty lines below already matched names from the message — the capability existed and the candidates block did not use it | Retrieval seeded by the names in the message: `findContactsNamedIn` fetches them regardless of rank and puts them FIRST, ranked rows fill the remainder to 80. Raising the cap was rejected — it only moves the cliff. Token extractor is a PREFILTER (model still chooses, `actionTargetGuard` still grounds), broader than the tone matcher's verb list because that list lacks "ask", which is the owner's actual phrasing. ANY token may match, so a first name alone resolves; email is not required, since a phone-only row is what a WhatsApp ask needs | `HEAD` | not yet | **FIXED (not deployed)** — root cause of every Hamna failure on 08-05 |
| **DEF-051** | 08-05 17:00, reworked 18:15 | 620 contacts with duplicates the nightly pruner never touched — "Hamna Latif Bhutta" and "Hamna ABAP TMC" sharing `+923134199294` survived every run | **Two duplicate detectors with different rules.** The Contacts UI groups by shared phone/email (`ContactsPage.jsx:741`), flags `⚠ Same as …` and offers Merge — it has known about these rows all along. The nightly pruner groups by **exact name**, so a WhatsApp pushname ("Hamna ABAP TMC") can never match a saved name. The only component able to act unattended was using the weaker rule. My earlier claim that flags "only write a log line" was wrong — the owner's screenshot disproved it. Recurrence is guaranteed by the ordinary sequence: someone WhatsApps before being saved (pushname row, correct), then the owner supplies the number for the contact he already had | Pruner now ALSO groups by shared email/phone, the same rule the UI uses. Safety unchanged and comes from `planMerge`: a group merges only when at most one distinct email and one distinct phone appear across it, so two people on one office number conflict and are flagged rather than merged. Normalised digits must match exactly — `contains` alone would over-match. **Still open:** the durable fix is a collision check in `update_contact` so the duplicate is never created | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-047** | 08-05 15:30 | **Brain has never read a single counterpart reply.** `delegation_thread_events` holds 15 events over 4 days — every one `outbound_intent`/`outbound_receipt`. **Zero inbound, ever** — while follow-ups kept going out daily to +923474937298 and +923028000553 | NOT a plumbing bug: `captureDelegationReply` is correctly called BEFORE the unregistered-sender drop (`WhatsAppInbound.ts:107`) and the flag IS on (`behavior.delegation.capture_enabled=1`). The blocker is the CORRELATION RULE — capture matches only on a quoted provider id, or **exactly one active thread for that counterpart**. Hamna has **two** active threads (`+923134199294`, both `awaiting_reply`), so a plain reply from her can never correlate. Counterparts are therefore being pinged on a daily schedule with no ability to detect that they answered | Correlate on the counterpart plus recency when several threads share a number, instead of refusing at >1. An unmatched reply from a known counterpart must still surface to the owner rather than fall through to unregistered-sender triage | `HEAD` | not yet | **FIXED (not deployed)** — correlation now picks the most recently contacted thread and DISCLOSES the guess; incident row still written |
| **DEF-048** | 08-05 15:30 | The owner's planned test — *"Ask Hamna whether she will come to office tomorrow"* — cannot work today, for three independent reasons | (1) `DelegationThread.openItemId` is **NOT NULL**, so a thread can only exist against an open item. An ad-hoc question to a counterpart has nowhere to be recorded. (2) Even tied to an item, Hamna's two active threads defeat the correlation rule (DEF-047). (3) `delegation.autonomous_outbound_enabled` is absent (0) and its own spec says *"no consumer exists"* — nothing reports an answer back to the owner | **Partly fixed.** (a) correlation solved by DEF-047; (b) the reporter now fires for `in_progress` — a counterpart answering the question reaches the owner instead of being filed silently. **Still open:** `openItemId` is NOT NULL, so an ad-hoc question with no open item still cannot be tracked. Route the owner's phrasing through open-item creation first (an unanswered question IS an open item) rather than making the column nullable — cheaper, and gives the question a status and a follow-up cadence | `HEAD` | not yet | **FIXED (not deployed)** — owner chose open-item routing (2026-08-05). `add_open_item` now carries an optional delegatee so "ask X whether Y" is ONE action: create + assign, which opens the thread that makes the reply readable. Assignment shared with `delegate_open_item` via `assignItemToCounterpart` (one implementation). An addressed item is no longer classified internal — externality read from slots, not type |
| **DEF-045** | 08-05 14:45 | Brain rendered a WhatsApp preview addressed to **`[unknown contact cmowq07k70gkqrtk53b8b1a8f]`**, the owner replied "send", and only THEN was it refused: *"I need to confirm the WhatsApp recipient — name the exact contact or give a valid number."* The owner approved a send that could never succeed | **The preview and the dispatcher run the SAME lookup and reach OPPOSITE conclusions.** `fmt()` in the preview renderer (`brainComposer.ts:5068`) calls `resolveCandidate`; on null it returns the string `[unknown contact <id>]` and shows the preview anyway — it degrades gracefully where it must fail closed. `candidateResolves` in `actionTargetGuard.ts:67` calls the same `resolveCandidate`; on null it refuses. **`verifyActionTargets` is never called on the preview path.** Fourth instance today of a protection existing in one place and not the other | Run `verifyActionTargets` BEFORE rendering any preview, not only before dispatch. An unresolvable target must produce a question ("which Hamna?") instead of a preview — which is also exactly the DEF-038 behaviour the owner asked for: ask when uncertain, act when certain. A preview is a promise that the action is ready to execute; it must not be shown otherwise | — | not yet | **OPEN — high** |
| **DEF-046** | 08-05 14:45 | Two DIFFERENT candidate ids referenced Hamna in one session: `cmoq9jmj701mxrtv65yomcw7p` (the follow-up policy stored at 14:10) and `cmowq07k70gkqrtk53b8b1a8f` (the WhatsApp send at 14:45, unresolvable). Delegation resolved her fine by email the same minute | **Hypothesis, not yet confirmed** — evidence is the two distinct ids plus the phone update at 14:09 ("Updated Hamna Latif Bhutta: phone → +923134199294") not being visible to the WhatsApp path. Suggests ≥2 contact records for one person, with different channels resolving to different rows. Needs a DB check before any code change | Confirm first: list every `entity_person` matching Hamna with id, email, phone, user_id, scope. If duplicated, merge and add a uniqueness guard at the writer. **Do not patch the resolver until the data is understood** — DEF-024 was a fix aimed at the wrong layer for exactly this reason | — | not yet | OPEN — diagnose before coding |
| **DEF-044** | 08-05 14:41 | Dispatch result printed **raw database ids**: `✓ Delegated "cmsernsi1000orto96r3kcmsk" to Hamna Latif Bhutta` ×3 — unreadable, and indistinguishable from each other | DEF-018 fixed the PREVIEW renderer to name items by title. The DISPATCH RESULT renderer is a different function and never got the same fix. **`protection-with-two-implementations` again, in its cosmetic form** — two renderers for the same concept, one fixed | The result line must resolve the same title the preview showed. Better: preview and result should share one item-label function, so a title fix cannot land in only one of them | — | not yet | OPEN |
| **DEF-042** | 08-05 14:08 | Email to Hamna had **no greeting, no line breaks, no list formatting**, signature merged into the body — nothing like how the owner actually writes to her | Tone sampling exists and is recipient-scoped (`getToneSamplesForRecipients`), but it is gated behind a **keyword regex on the owner's phrasing**: `/\b(email\|send\|reply\|draft\|write\|note\|message)\b/i.test(question)` (line 1901). The owner said *"you should also update them that these items have been dedicated to her/him"* — not one of those words appears, so `toneContextBlock` was empty and the LLM wrote from a blank slate. **A regex decided whether Brain gets to sound like the owner** — the `no-hardcoded-judgement` invariant, DEF-013 class | Drop the keyword gate. If an outbound message to a resolved recipient is being composed, that IS the trigger — decide from the *action being taken*, not from the words the owner happened to use. Sample from the same channel only (email samples for email) per the channel-tone separation rule | — | not yet | OPEN — owner-requested |
| **DEF-040** | 08-05 14:00 (external audit) | **`fetch_contact_full` returns another user's private contact.** Email, phone, saved-phonebook name, provenance and the other owner's user id — all readable by any user in the tenant | Verified in source: all three lookup limbs filter on `client_number` only — no `user_id`, no `scope` predicate (`brainTools.ts:377`). Worse than a missing filter: line 411 *detects* foreign ownership (`another user (id ${row.userId})`) and renders the payload anyway. `resolveWhatsAppContact` (`brainTools.ts:270`) has the same missing scope filter. **Recurrence of the class fixed on 2026-05-25** — that was a WRITER misattributing rows, this is a READER with no scope predicate | Structural, not a patched WHERE clause: one shared scoped-contact accessor that every reader must use, enforcing `userId = requester OR scope = 'tenant'` (contacts are private by default; tenant visibility is owner-only opt-in). CI guard naming it sole owner, same shape as `waIdentity.ts` and DEF-039's chokepoint | **OPEN — cross-user data exposure, second highest priority after DEF-039** |
| **DEF-038** | 08-05 13:50 | **Brain demands "send" for an instruction the owner already gave.** "delegate these to hamna latif" is an instruction, not a request for a proposal — the extra confirmation adds no authority (`SendProvenance` is already satisfied by the sentence) and adds no information (the preview restates the owner's own words) | Preview-before-dispatch is applied as a BLANKET rule on every action. It was the containment for a Brain that could not ground its targets (DEF-002, DEF-004) — a job `actionTargetGuard` now does structurally in CI. The manual gate outlived its reason. Worse: the confirm step is itself the home of DEF-024, DEF-032 and DEF-035 — the safety mechanism became the primary failure mode | **Owner rule (CL-024):** confirm only when the request is *not normal* or carries *risk*, and only after NAMING the risk. Otherwise act and report after. "Not normal" = deviation from history (new delegatee, unusual volume) so a proven Brain asks less over time; "risk" = blast radius × reversibility × whose identity × input solidity. Facts read from the DB, verdict from the LLM — never a regex (DEF-013 class). **Uncertainty about risk is itself a trigger to ask**, so a bad risk judgement cannot fail silently into a real message. The ask must name the reason ("Hamna has not been a delegatee before"), never "Shall I proceed?" | OPEN — **highest-value product change in the queue** |
| **DEF-037** | 08-05, fixed 19:10 | **Brain could not see its own past.** *"did you inform hamna about items delegated to her?"* → *"No Sir, I have not… I don't have a contact entry for 'Hamna Latif'"* — one hour after emailing her (`messageId 19fd12e8abc16d71`), holding both her email and phone | The only dispatch view reaching the prompt was `renderArtifactsBlock(history)` — a filter over the CONVERSATION TRANSCRIPT for `role==='artifact'` turns. Once an action leaves the trimmed history window it is invisible, so the model answers "did I?" by inference. **The row was in `brain_action_artifacts` the whole time.** Same class as DEF-019 (fabricated "connection degraded" while the DB said connected 4 days) and both DEF-034 denials | `dispatchLedgerService.buildDispatchLedgerBlock` — 36h of real dispatches read from the DB, newest first, with delivery ticks folded in from DEF-052. Passed as its own `dispatchLedger` block and rendered **even when empty**, because "(no actions dispatched)" is a fact and omitting it restores the guessing. Prompt rule makes it the ONLY valid source for "did you…?". Nothing in the block can be fabricated: every line is a row with an id and a timestamp | `HEAD` | not yet | **FIXED (not deployed)** |
| **DEF-036** | 08-05 13:35 | **`compose()` is 2,176 lines** (`brainComposer.ts` 1713→3889; the file is 6,342 lines = 24% of the 26,436-line knowledge layer, 6× the next largest file) | No structural cause beyond accretion. But it is a defect GENERATOR, not just debt: DEF-035 was the confirm reducer sitting ~200 lines below the reasoning gate and therefore unreachable — an ordering bug that is only possible inside a function nobody can hold in their head. DEF-032 (silent displacement) is the same shape | Decompose into an explicit ordered pipeline of named stages over a typed turn state, so stage order is declared data rather than an accident of line number, and a stage cannot be unreachable without CI noticing. The 82-module, ~245-line-average layer around it is healthy — this is one pathological file, not a systemic layout problem | OPEN — **prerequisite for DEF-038**; both live in the same unreadable region |
| DEF-019 | 08-03 17:25 | Brain fabricated "my WhatsApp connection is currently degraded" while the DB said `connected` for 4 days | channel health inferred instead of read from a ground-truth block | supply health as a data block; forbid inferring it | OPEN |
| DEF-020 | 08-04 19:58 | Machine-translated pronoun treated as the owner's assertion: Urdu "unko" → "him" → Brain argued the owner called Hamna "he" (3 turns lost) | translated transcripts carry invented gender; composer isn't told the text is a translation | mark translated transcripts; never infer/challenge gender from them | OPEN |
| DEF-021 | 08-04 19:59 | Corrected title reverted — "ShireMe Recruiting Portal" confirmed, then "Shiny Recruitment Portal" a minute later | reasoning re-reads stale STT text instead of the persisted title | read titles from the item record only | OPEN |
| DEF-022 | 08-04 19:58 | Queue interrupted mid-conversation asking for a priority already dictated twice; hint offered date phrases for a priority question | no conversation-turn suppression; mismatched prompt copy | suppress during an active turn; fix copy per slot kind | OPEN |
| DEF-026 | multiple | "Morning, Sir" at 3:30 PM / 5:26 PM / 8:30 AM | prompt gets `getUserLocalDate` (date, no clock) while the persona few-shot opens "Morning, …" | inject local TIME; neutralise the few-shot | OPEN (`time-of-day-drift`) |
| DEF-027 | multiple | All-day events rendered with a time ("Office at 5 AM") | all-day events not distinguished in the calendar view | render all-day as all-day | OPEN |
| DEF-028 | 07-28 | `whatsapp_messages.error_message` NULL on every failed row | failure reason discarded at the write path | persist the reason | OPEN (observability) |
| DEF-029 | every boot | Alert email dead — `535 Authentication unsuccessful` for basit.ahmed@tmcltd**.com** | SMTP credentials/domain | fix credentials | OPEN — **this is why the 6-day outage went unnoticed** |
| **DEF-034** | 08-05 11:56, **RECURRED 08-05 14:41** | Brain said "Hold on — I hadn't actually done that yet; nothing was executed on my end. Say the word and I'll do it now" **one second after successfully delegating three items** | **ROOT CAUSE FOUND 14:41, and my own DEF-041 fix is what exposed it.** Chain: send #1 dispatched and marked the pending terminal → send #2 found no active pending → fell through to reasoning → reasoning re-claimed the completion → **the empty-promise rule fired (correctly, now that DEF-041 un-exempted it)** → blocked the answer with `[no action dispatched…]` → `answerSanitizer.ts:39` renders that marker as **"nothing was executed on my end"**. The marker is TURN-scoped ("no action dispatched *in this turn*"); its human rendering is WORLD-scoped ("nothing was executed"). Both statements are about different things and only the first is true. Exactly the DEF-037 provenance failure: the system cannot distinguish *I didn't do this in this turn* from *this was not done*. **Note honestly: before DEF-041 this rule never fired on the reasoning path, so my fix increased the frequency of this false denial** | (a) Reword the marker to be turn-scoped — "I didn't run anything just now", never "nothing was executed"; (b) before emitting it, read the dispatch ledger for a recent completion of this pending — if the work IS done, the honest answer is "that's already done", not a denial; (c) **never offer "say the word and I'll do it now" for work already completed** — that is an invitation to duplicate a real external action | — | not yet | **OPEN — CRITICAL, actively firing on production** |
| DEF-031 | 08-04 20:22 | Junk task **"Priority High (due yesterday)"** still in the open-items list | data debris created by the DEF-017 compound-command bug on 08-03 | archive the row; no code change | OPEN — needs a one-line cleanup, owner's call |
| DEF-030 | 07-28 | Owner's personal WA client down — `[fetchThreadContext] no client in user map, userId 2` | `UserWebjsProvider` session not initialised | re-pair / auto-init | OPEN |

**Absent by design, not defects:** WhatsApp calling (no foundation; owner excluded it).

---

## C. Builder-process defects (mine, not Brain's)

Recorded because they cost the owner real time and must not repeat.

| ID | Date | What I did wrong | Consequence | Correction |
|---|---|---|---|---|
| BLD-001 | 08-05 11:40 | Used `git add server/docs/` (directory add) instead of naming files | Committed and pushed `server/docs/nexeo_self_learning&development.md` — a file AGENTS.md explicitly forbids ever committing — and it landed on production via the pull | `git rm --cached` (kept on disk), added to `.gitignore`. **Rule: always name files in `git add`, never a directory.** History still contains it; purge available on request |
| BLD-002 | 07-28→08-04 | Carried the open-defect queue in conversation instead of a document | Made months of real progress look like circling to the owner | The three-doc protocol |
| BLD-003 | 08-04 | Reported a suite figure measured BEFORE the final edit | Claimed green while the tree was red | Re-run after the last change; quote commit-stamped numbers |
| **BLD-005** | 08-05 13:12 | Wrote a branch that deliberately bypasses another branch, and did not check what protections that branch was carrying. The DEF-035 guard skipped the confirm reducer to fix ordering — and skipped its idempotency wrapper and artifact ledger with it | Shipped a duplicate-send path to production for ~30 minutes and told the owner to run a "send" test on it. An external audit caught it, not me and not 1,272 tests | **When bypassing a code path, enumerate what it does BEYOND the thing you are bypassing.** A guard that lives inside one caller is not a guard. Structural answer: extract the chokepoint and CI-enforce a single caller, rather than duplicating the protections into the new branch |
| BLD-004 | 08-04 | Left DEF-024's fix pushed but undeployed while diagnosing other things | The same defect hit the owner three separate times | Deploy blockers before starting new diagnosis |

## D. Undeployed work waiting on the box

All of `DEF-018`, `DEF-023`, `DEF-024`, `DEF-025` were **deployed 2026-08-05 11:40** at HEAD
`7ace0f5`. Nothing is waiting on the box.

Awaiting live exercise: does "send" dispatch (DEF-024), do previews name items (DEF-018),
do plan steps update (DEF-023), does native typing appear (DEF-025). `DEF-024` includes a migration
(`20260804_pending_unique_any_name`), so the deploy needs `prisma migrate deploy`.
