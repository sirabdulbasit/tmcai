# Session Handoff — 2026-08-05 (Nexeo Brain: confirmation loop root-caused)

Paste the block at the bottom of this file as the opening prompt of a new session.

---

## 1. State right now

| | |
|---|---|
| Branch | `feat/nexeo-one-brain` |
| Local + origin HEAD | **`1d4a1d3`** |
| **Production HEAD** | **`4ec5d99`** — 3 commits behind |
| Suite | **1272 passed / 21 skipped / 0 failed**, `tsc` clean, `npm run build` clean |
| WhatsApp channel | `connected`, stable since 07-31 (5+ clean reconnects) |

**Undeployed (this is the first thing to do):**

| Commit | Fixes |
|---|---|
| `1d4a1d3` | **DEF-035** — the "send → same preview" loop (ordering) |
| `1880538` | DEF-032 silent plan displacement · DEF-033 test-email hijack |
| `1ab85f6` | docs only |

## 2. The three governing documents — READ FIRST, UPDATE EVERY SESSION

Owner ruling 2026-08-05. These are not optional and they exist because the defect
queue used to live only in chat, which made real progress invisible.

| Doc | Job |
|---|---|
| `server/docs/brain_change_log.md` | Errors → root cause → solution → commit → **deployed?** → verification. `DEF-NNN`. Also `§C` = builder-process defects (`BLD-NNN`). |
| `server/docs/brain_chat_log.md` | Every chat log the owner sends, `CL-NNN`, date-time, what was OBSERVED, plus **what went right**. |
| `server/docs/brain_evaluation_chart.md` | Per-deploy before/after. Capability state, recurrence table, trend. **Reported at EVERY deploy** in the §2.1 five-line format. |

**Rules:**
1. Log an owner-reported issue in the change log the **same session** it arrives.
2. Before coding, check the recurrence table and **state the verdict out loud**:
   *progressing* (new root cause) or *circling* (tag already there ⇒ **structural** fix
   required, never another patch on the reported instance).
3. `DEPLOYED` ≠ `VERIFIED`. Anything not exercised on production says **not verified**.
4. Report the evaluation chart at every deploy: Deploy · Intended · Verified (with
   evidence) · Still broken · Verdict + next action.

Deeper context: `server/docs/brain_chat_archive.md` (Chat 1–17 post-mortems, each paired
with an executable `chatN` scenario in `server/tests/brainScenarios.ts` — a meta-test fails
if one exists without the other).

## 3. Immediate next action — deploy D-8 and verify

```bash
cd /var/www/tmcai && git pull --ff-only && git rev-parse --short HEAD
cd /var/www/tmcai/server && npm run build && pm2 restart tmcai-server
```

HEAD must read `1d4a1d3`. No migration. **Never** `pm2 restart --update-env` (poisons
`DISPLAY`, breaks Xvfb). Ignore the Prisma 6→7 upgrade banner.

**How to test — the exact sequence that has failed three times:**
1. `list down only those which are not delegated yet`
2. `delegate these to hamna latif` → preview should NAME each item
3. **`send`** → *success* = real delegation lines · *failure* = the preview again
4. `what is delegated to Hamna?` → the items should now appear

That single sequence verifies **DEF-035, DEF-023, DEF-024** together. Then record the
result in the evaluation chart (D-8 row) and flip the DEF rows to VERIFIED (LIVE).

## 4. Verified working on production (do not "fix" these)

- Voice notes read + transcribed, **Urdu → English**, transcript echoed as `🎙️ Heard:`
  (DEF-016, LIVE 08-04 19:37)
- Compound voice dictation decomposed into a correct multi-step plan (DEF-017, LIVE 08-05)
- Previews name each item and every changed field (DEF-018, LIVE 08-05)
- Channel survives reconnect; delegation sends reach counterparts (DEF-014, LIVE 07-31)
- `.env` pins live: `BRAIN_INPUT_TRANSLATE_DEFAULT=english`,
  `BRAIN_REPLY_LANGUAGE_DEFAULT=english`

## 5. Open defects — full detail in the change log §B

Highest value first:

| DEF | Symptom |
|---|---|
| DEF-034 | Claimed "nothing was executed on my end" **immediately after sending an email** — status inferred, not read from the dispatch ledger (same family as DEF-019) |
| DEF-019 | Fabricated "my WhatsApp connection is degraded" while the DB said `connected` for 4 days |
| DEF-020 | Machine-translated pronoun treated as the owner's assertion (Urdu "unko" → "him" → argued the owner called Hamna "he") |
| DEF-021 | Corrected title reverts to stale STT text |
| DEF-022 | Prompt queue interrupts mid-conversation; asks for a priority already given; wrong hint copy |
| DEF-026 | "Morning, Sir" at 1 PM — prompt gets the date but no clock; persona few-shot opens "Morning" |
| DEF-027 | All-day events rendered with a time ("Office at 5 AM") |
| DEF-028 | `whatsapp_messages.error_message` NULL on every failed row |
| DEF-029 | **Alert email dead** (`535` for basit.ahmed@tmcltd**.com**) — why a 6-day outage went unnoticed |
| DEF-030 | Owner's personal WA client down (`no client in user map, userId 2`) |
| DEF-031 | Junk item "Priority High" + Leave Request dated **Mar 29 2024** — data debris; owner's call to archive |

**Absent by design:** WhatsApp calling. No foundation; owner excluded it. Do not propose it.

## 6. Do NOT re-chase these (disproved with evidence)

- **Voice `r: r` is not** a missing module, not an unreleased upstream fix (1.34.7 already
  has `downloadQpl`), and not fixable by rotating the pinned WhatsApp Web build. Root cause
  was the `@lid`-embedded message id poisoning `Msg.get`. Fixed.
- **`@lid` recurred 4×** with four different root causes. Containment is structural:
  `whatsapp/waIdentity.ts` is the ONLY module allowed to call the mapping API (CI-enforced),
  and `webjsMediaDirect` / `webjsChatStateDirect` bypass id parsing entirely. A **fifth**
  recurrence means containment failed — record it in the chart before touching code.
- **Meta Cloud API** is parked by owner decision (`brain_change_log`, CL-019 era). The code
  path exists and works; do not push it again unless asked.

## 7. Hard rules (violating these has already cost real time)

- **Never `git add <directory>`** — always name files. A directory add committed
  `server/docs/nexeo_self_learning&development.md`, which AGENTS.md forbids (BLD-001).
- **Never commit:** `.env`, tokens, `whatsapp-sessions/`,
  `server/docs/nexeo_self_learning&development.md`, `tenant-scope-audit-2026-05-22.md`.
- **Owner runs every production command.** Every deploy block ends with a **"How to test"**
  section stating success vs failure.
- **Re-run the suite AFTER the last edit.** A figure measured before the final change was
  once reported as green while the tree was red (BLD-003).
- **Deploy blockers before starting new diagnosis.** DEF-024 hit the owner three times
  while its fix sat pushed-but-undeployed (BLD-004).
- **Source-guard tests must strip comments** before asserting — explanatory prose matched
  its own assertion four separate times.
- Verification matrix from `server/`: `npx tsc --noEmit` · `npx vitest run` ·
  `npm run build` · `git diff --check`.

## 8. Owner working style

Short replies, one command per message, phone-screen length. Wants root causes, not
patches; says so directly when he feels we are circling. Answer his actual question before
volunteering next steps. Product name is **Nexeo**; his AI is named **Suzi** (configurable —
never hardcode it).

---

## PROMPT FOR THE NEW SESSION — paste from here

```
Continue work on Nexeo (tmcai). Read these first, in order:

1. HANDOFF_2026-08-05.md (repo root) — full state
2. server/docs/brain_change_log.md — DEF-NNN registry, open queue in §B
3. server/docs/brain_chat_log.md — CL-NNN, what the owner observed
4. server/docs/brain_evaluation_chart.md — per-deploy before/after, recurrence table
5. AGENTS.md + CLAUDE.md — working contract and invariants

Working protocol (owner ruling, non-negotiable):
- Every owner-reported issue gets a DEF row the SAME session. Never carry it in chat only.
- Before coding, check the recurrence table and SAY whether we are progressing (new root
  cause) or circling (tag already present ⇒ structural fix required, not a patch).
- Update all three docs with every change; report the evaluation chart at EVERY deploy
  (Deploy · Intended · Verified with evidence · Still broken · Verdict + next action).
- DEPLOYED is never the same as VERIFIED. If it wasn't exercised on production, say so.

State: branch feat/nexeo-one-brain, local/origin HEAD 1d4a1d3, PRODUCTION on 4ec5d99 —
3 commits behind. Suite 1272 passed / 0 failed.

FIRST TASK: hand me the deploy block for 1d4a1d3, then verify DEF-035 — the
"send → same preview → send → same preview" loop. Root cause was ordering: in compose(),
the reasoning gate ran before the confirm reducer, so every "send" re-proposed the plan and
displaced the one it was meant to confirm. An early-confirm guard now dispatches the stored
preview before any re-reasoning.

Test sequence (has failed three times, verifies DEF-035 + DEF-023 + DEF-024 together):
  1. "list down only those which are not delegated yet"
  2. "delegate these to hamna latif"  → preview must NAME each item
  3. "send"  → success = real delegation lines; failure = the preview again
  4. "what is delegated to Hamna?"  → items should appear

I run all production commands myself. Keep replies short — one command per message.
Do not re-chase anything in HANDOFF §6 (disproved with evidence). WhatsApp calling is
out of scope by my decision.
```
