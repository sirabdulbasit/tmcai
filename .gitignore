node_modules/
dist/
.env
*.env.local
*.env.production

# GCP service account key (NEVER commit)
tmcai-key.json

# Google OAuth client secret (NEVER commit)
client_secret_*.json

# Vector store (contains indexed business data)
server/data/vectors.json

# Google Drive tokens
server/.gdrive_token.json

# Logs
*.log
logs/

# Database dumps (never commit)
*.sql.dump
tmcai_data.sql

# OS files
.DS_Store
Thumbs.db

# PM2 logs
/var/log/tmcai/

# WhatsApp Web.js session cache + auth (device-specific, re-scan QR on new machine)
server/.wwebjs_cache/
server/.wwebjs_auth/
server/whatsapp-sessions/

# Local Claude Code settings (machine-specific)
.claude/

# Reference binaries (optional — uncomment to exclude large specs from repo)
# reference/*.pdf
# reference/*.docx
